spring:
  application:
    name: notification
  docker:
    compose:
      enabled: true
      file: docker-compose.yml    
  cloud:
    config:
      uri:
      - http://localhost:8888
      fail-fast: true