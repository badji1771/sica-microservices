spring:
  application:
    name: reglement
  docker:
    compose:
      enabled: true
      file: docker-compose.yml
  cloud:
    config:
      uri:
        - http://localhost:8888
      fail-fast: true
